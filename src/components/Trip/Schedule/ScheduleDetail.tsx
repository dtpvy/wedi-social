import { ScheduleDetail } from '@/types/schedule';
import React from 'react';

type Props = {
  schedule: ScheduleDetail;
};

const ScheduleDetail = ({ schedule }: Props) => {
  return <div>Schedule</div>;
};

export default ScheduleDetail;
